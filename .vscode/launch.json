{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Hidden EXE",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\.vscode\\build.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "preLaunchTask": "build-hidden-exe"
        }
    ]
}
